apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloudops-api
  namespace: cloudops
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cloudops-api
            port:
              number: 80
